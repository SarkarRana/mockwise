{
  "name": "mockwise-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "Realistic, edge-case-aware test fixtures from your Zod or Pydantic schemas.",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=20"
  }
}
